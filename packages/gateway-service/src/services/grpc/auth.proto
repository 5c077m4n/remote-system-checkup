syntax = "proto3";

package auth;

service AuthService {
	rpc Hash (Password) returns (HashedPassword) {}
	rpc Verify (Password) returns (IsValid) {}
}

message Password {
	string content = 1;
}
message HashedPassword {
	string content = 1;
}
message IsValid {
	bool content = 1;
}
