syntax = "proto3";

package auth;

service AuthService {
	rpc Verify (User) returns (IsValid) {}
}

message User {
	string username = 1;
	string password = 2;
	string email = 3;
	string secret = 4;
}
message IsValid {
	bool content = 1;
}
